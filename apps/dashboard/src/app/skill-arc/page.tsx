// Hero page — built out in A.2. Placeholder so the nav link doesn't 404.
export default function SkillArcPage() {
  return (
    <section className="space-y-2">
      <h1 className="text-3xl font-semibold tracking-tight">Skill arc</h1>
      <p className="text-muted-foreground">
        Live tick coming in A.2 (Recharts + SWR every 2s).
      </p>
    </section>
  );
}
